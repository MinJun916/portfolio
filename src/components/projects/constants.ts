/** 프로젝트 상세 페이지 콘텐츠 최대 너비(px). 변경 시 아래 CLASS 문자열도 동일 값으로 수정 */
export const PROJECT_CONTENT_MAX_WIDTH_PX = 1200;

/** Tailwind용 클래스 (위 상수와 동일한 px 값으로 유지) */
export const PROJECT_CONTENT_MAX_WIDTH_CLASS = 'max-w-[1200px]';
